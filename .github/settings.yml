# .github/settings.yml
# These settings are synced to GitHub by the Repository Settings app.
# Install the app: https://github.com/apps/settings
# Docs: https://github.com/repository-settings/app

repository:
  # The name of the repository. Changing this will rename the repository
  name: my-repo-name

  # A short description of the repository that will show up on GitHub
  description: "A short, clear description of your project."

  # A URL with more information about the repository
  homepage: https://example.com

  # A comma-separated list of topics to set on the repository
  topics: github, yaml, settings, automation

  # Either true to make the repository private, or false to make it public
  private: false

  # Either true to enable issues for this repository, false to disable them
  has_issues: true

  # Either true to enable projects for this repository, or false to disable them
  # (If projects are disabled for the organization, passing true will cause an API error)
  has_projects: true

  # Either true to enable the wiki for this repository, false to disable it
  has_wiki: false

  # Either true to enable downloads for this repository, false to disable them
  has_downloads: true

  # Updates the default branch for this repository
  default_branch: main

  # Either true to allow squash-merging pull requests, or false to prevent it
  allow_squash_merge: true

  # Either true to allow merging pull requests with a merge commit, or false to prevent it
  allow_merge_commit: false

  # Either true to allow rebase-merging pull requests, or false to prevent it
  allow_rebase_merge: false

  # Either true to enable automatic deletion of branches on merge, or false to disable
  delete_branch_on_merge: true

  # Either true to enable automated security fixes, or false to disable
  enable_automated_security_fixes: true

  # Either true to enable vulnerability alerts, or false to disable
  enable_vulnerability_alerts: true

# Optional: Labels
labels:
  - name: bug
    color: d73a4a
    description: Something isn't working
  - name: enhancement
    color: a2eeef
    description: New feature or request
  - name: documentation
    color: 0075ca
    description: Improvements or additions to documentation

# Optional: Collaborators (organization-owned repos)
# collaborators:
#   - username: octocat
#     permission: push   # pull, triage, push, maintain, or admin

# Optional: Teams
# teams:
#   - name: my-team
#     permission: push
